name: Pages Deploy (disabled)

# This workflow was disabled because it duplicated the main Pages deploy workflow
# and caused YAML validation errors. Kept as a placeholder in case you want to
# re-enable or use it later. To re-enable, restore the original deploy steps.

on: {}
